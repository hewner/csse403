A small little activity that illustrates Rust's async await.

Modify this code to use a second mutex that is used by returns_three.  Verify that regardless of which mutex is locked, the code progresses as much as it can.